import React from 'react';
// import { useState } from 'react';
// import Nav from './Nav';

function Header ({children}) {
    return (
      <header>
        {children}
      </header>
    );
  };


export default Header;
