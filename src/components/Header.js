import React from 'react';
import { useState } from 'react';
import Nav from './Nav';

function Header () {
    return (
      <header>
        <Nav></Nav>
      </header>
    );
  };


export default Header;
